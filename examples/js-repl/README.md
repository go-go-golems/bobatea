# JavaScript REPL Moved

This example has been retired from Bobatea.

The JavaScript/Goja-specific REPL now lives in `go-go-goja` as:

```bash
go run ./cmd/js-repl
```

Bobatea continues to own and provide the generic reusable REPL shell and widgets.
